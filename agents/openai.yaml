interface:
  display_name: "auto-video-edit"
  short_description: "五种 Remotion overlay 风格保真复用"
  default_prompt: "Use $auto-video-edit to generate a Remotion overlay with one of the five bundled style packs, preserving the selected style's tokens, components, and geometry."
